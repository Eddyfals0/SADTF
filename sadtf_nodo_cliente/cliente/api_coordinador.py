"""Funciones para hablar con el coordinador (stub)."""
import requests

def put_file(coordinador_url, metadata):
    print(f"PUT_FILE -> {coordinador_url} : {metadata}")

def get_file(coordinador_url, file_id):
    print(f"GET_FILE -> {coordinador_url} : {file_id}")
